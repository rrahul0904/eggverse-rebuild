{
  "name": "eggverse-rebuild",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency social browser arcade rebuild with 18 games and shareable multiplayer rooms.",
  "scripts": {
    "dev": "node server.mjs",
    "start": "NODE_ENV=production node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=22" }
}
